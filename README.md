# Srujan N S — Portfolio (React + Vite + Tailwind)

## Quick Start

1. Install dependencies:
   ```
   npm install
   ```

2. Run dev server:
   ```
   npm run dev
   ```

3. Build for production:
   ```
   npm run build
   ```

4. Preview production build:
   ```
   npm run preview
   ```

## Notes
- Profile image should be placed at `public/assets/profile.jpg`.
- Resume PDF is included at `public/assets/Srujan_N_S_Updated_V2.pdf`.
- Deploy to Vercel or Netlify by connecting your GitHub repo; both detect Vite projects automatically.
